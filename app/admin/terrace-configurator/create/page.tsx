import CreateProductView from '@/view/admin-panel/products/create-product'
import CreateTerraceConfigView from '@/view/admin-panel/terrace-config/create-terrace-config'

export default async function CreateTerraceConfigPage() {
    // to do fetch
    return <CreateTerraceConfigView />
}
